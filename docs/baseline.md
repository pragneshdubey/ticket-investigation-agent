# Baseline Classifier

The baseline makes one LLM call and predicts only the ticket category and priority. It uses no tools and has no agent loop, duplicate detection, verification, or human review. It is the control used to compare later agent versions.